Დავალება 3

მოცემულია მასივის ელემენტები:

let students = ["ana", "levani", "gio", "nuca", "alexandre", "ika", "sandro", "lizi"];

for ციკლის გამოყენებით გადაუარეთ მასივს და შექმენით ახალი მასივი იმ ელემენტებისგან, რომელთა სიგრძე მეტია 4-ზე.

მიღებულ ახალ მასივს დაუმატეთ ერთი ელემენტი დასაწყისში და ერთი ელემენტი ბოლოში.

დაბეჭდეთ საბოლოო მასივი კონსოლში.

მოცემულია მასივი:

let ages = [12, 25, 18, 31, 7, 44, 29, 16];

მასივის ჩაშენებული ფუნქციის გამოყენებით გაფილტრეთ მხოლოდ კენტი რიცხვები და დაბეჭდეთ კონსოლში.

მოცემულია ორი მასივი:

let arrayOne = [15, 3, -8, 22, 1];
let arrayTwo = [7, -2, 30, 11, 5];

შეაერთეთ ეს ორი მასივი.

მიღებული მასივი დაალაგეთ კლებადობით.

for...of ციკლის გამოყენებით დაბეჭდეთ თითოეული ელემენტი კონსოლში.
